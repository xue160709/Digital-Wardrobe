# 开发计划

## 1. 项目框架
- 工具形态：移动 App
- 主框架：Next.js（移动端优先响应式）
- 框架映射：移动 App → Next.js（App Router + TypeScript）
- 选择理由：Next.js App Router 支持 SSR、首屏快；React 生态适合图像处理；便于后续扩展为 PWA 或 Capacitor 原生 App
- Agent 启用矩阵：
  - 必选：mvp-requirement-analyst、project-architecture-planner、qa-acceptance-planner
  - 条件启用：ui-interaction-planner（网站有可视入口）、api-integration-planner（涉及远程 AI 服务）
  - 未启用：platform-capability-planner（非桌面软件/浏览器插件）

## 已完成模块（详见 prd/）

> 首版留空。某 §2.x 功能全部 `[x]` 且经 project-verify 验证后，按 `.claude/rules/todo-prd-archive.md` 写入对应 `prd/<模块>.md`，并在此追加索引；§2 中该块折叠为「已归档 → prd/…」。

（暂无）

## 2. MVP 功能拆解

### 2.1 智能衣橱录入
- 用户目标：快速将衣服数字化录入系统，通过 AI 自动识别减少手动输入成本
- 使用入口：首页「+」按钮 / 衣橱页面顶部「添加衣服」
- 子功能：
  - [x] 拍照或从相册选择图片
  - [x] AI 自动识别衣服类别（上衣/裤装/裙装/外套/鞋配）
  - [x] AI 自动识别颜色（色系+明度）
  - [x] AI 自动识别风格标签（简约/休闲/正式/甜美/酷帅等）
  - [x] 手动修正 AI 识别结果
  - [x] 保存入库并展示在衣橱列表
- 主流程：
  1. 用户点击「+」→「选择拍照」或「从相册选择」
  2. 用户拍照/选图 → 进入识别中状态
  3. AI 识别返回类别+颜色+风格 → 展示识别结果卡片
  4. 用户可手动修正标签 → 确认保存
  5. 保存成功 → 自动返回衣橱列表，新衣服出现在顶部
- 状态要求：
  - 默认态：「+」按钮悬浮于页面右下角（Primary #BE185D）
  - 空态：衣橱为空时显示引导插图 + "还没有衣服，添加第一件吧"
  - 加载态：拍照完成后显示 "AI 识别中..." + 进度动画
  - 成功态：识别结果以卡片形式展示，标签可点选修改
  - 错误态：识别失败时显示 "识别失败，请手动选择类别"
- 数据需求：图片文件（JPG/PNG）+ AI 返回标签 + 用户修正结果 → 内存存储
- MVP 边界：单张拍照/选图、AI 自动打标签（mock）、手动修正、保存至列表
- 暂不实现：批量导入/批量拍照、衣服编辑页、删除衣服功能、品牌识别
- 验收标准：
  - [x] 点击「+」可调起相机拍摄
  - [x] 拍照后 AI 自动识别并展示类别/颜色/风格标签
  - [x] 用户可手动修改 AI 识别的标签
  - [x] 录入完成后衣服出现在衣橱列表顶部
  - [x] 衣橱列表支持按类别筛选
  - [x] 衣橱列表支持关键词搜索

### 2.2 风格分析报告
- 用户目标：直观了解自己的穿衣偏好和风格倾向，有参考标准指导后续购物和搭配
- 使用入口：底部 Tab「风格报告」
- 子功能：
  - [x] 衣橱数据统计（各品类数量、颜色分布、风格占比）
  - [x] 偏好色系分析（按颜色频率加权）
  - [x] 风格关键词提取（出现最高的 3-5 个风格标签）
  - [x] 可视化报告生成（图表：饼图+柱状图）
  - [x] AI 解读文案（一段话总结）
- 主流程：
  1. 用户进入风格报告页 → 检查衣橱衣服数量
  2. 若 < 5 件 → 提示 "衣服太少，添加更多衣服获得准确分析"
  3. 若数量充足 → 显示加载状态 "分析中..."
  4. 生成可视化报告 → 展示色系饼图、风格柱状图、关键词
  5. 底部显示 AI 解读文案
- 状态要求：
  - 默认态：展示历史生成报告（最新一次）
  - 空态：衣橱 < 5 件时显示 "上传更多衣服，生成精准报告"
  - 加载态："正在分析你的衣橱..." + 进度指示
  - 成功态：报告页完整展示（图表+解读）
  - 错误态：分析失败时显示 "生成失败，请稍后重试"
- 数据需求：读取衣橱中所有衣服的 category、color、style 字段
- MVP 边界：至少 5 件衣服触发分析、颜色饼图+风格柱状图、基础解读文案
- 暂不实现：历史报告对比、报告分享、深度趋势分析
- 验收标准：
  - [x] 进入「风格报告」页面，页面正常渲染无崩溃
  - [x] 报告展示穿衣偏好（如：简约风格占比 60%）
  - [x] 报告展示色系倾向（如：莫兰迪色系占比 45%）
  - [x] 报告展示风格关键词标签
  - [x] 报告包含可视化图表（饼图+柱状图）
  - [x] 衣橱衣服数量 < 5 件时，页面提示「请上传至少 5 件衣服以生成报告」

### 2.3 深度形象分析
- 用户目标：通过 AI 获得针对个人肤色、身材的专属风格定位和穿搭建议
- 使用入口：底部 Tab「形象分析」
- 子功能：
  - [x] 肤色测试（拍照或选择标准照，AI 判断冷暖/深浅）
  - [x] 身材分析（手动选择身形类型：苹果/梨形/沙漏/H型/倒三角）
  - [x] 风格定位（结合肤色+身材+偏好，给出风格关键词）
  - [x] 穿搭建议（按场景：通勤/约会/休闲给出颜色禁忌、款式推荐）
  - [x] 分析结果本地存储（下次打开可直接查看）
- 主流程：
  1. 用户进入形象分析页 → 展示引导页（说明目的+隐私说明）
  2. 肤色测试：用户选择肤色类型
  3. 身材分析：用户手动选择身形 → 系统记录
  4. 综合分析：结合肤色+身材+衣橱风格偏好 → 生成风格定位
  5. 展示穿搭建议（按场景）
- 状态要求：
  - 默认态：显示上次分析结果或引导页
  - 空态：新用户显示引导页 "了解你的专属风格"
  - 加载态："分析中..." + 步骤进度
  - 成功态：完整展示肤色类型+身形类型+风格定位+穿搭建议
  - 错误态：肤色识别失败时提示 "照片不符合要求，请重新拍摄"
- 数据需求：肤色类型、身形选择、分析结果 → 内存存储
- MVP 边界：肤色测试+身材手动选择+风格定位+通勤+休闲两个场景的穿搭建议
- 暂不实现：实时视频动态分析、换季提醒、护肤品推荐
- 验收标准：
  - [x] 进入「形象分析」页面，可开始肤色测试
  - [x] 肤色测试完成后展示结果（如「暖色调 - 浅春型」）
  - [x] 身材分析提供身形选项，用户手动选择
  - [x] 综合肤色+身材，生成专属风格定位（如「都市文艺风」）
  - [x] 展示穿搭建议（至少通勤+休闲两个场景）
  - [x] 测试结果持久化，关闭 App 后再次打开仍可查看

### 2.4 每日穿搭推荐
- 用户目标：节省每日搭配时间，根据天气和场景快速获得可出门的穿搭方案
- 使用入口：底部 Tab「今日穿搭」
- 子功能：
  - [x] 天气数据获取（接入天气 API 获取温度/天气状况）
  - [x] 场景选择（通勤/约会/休闲/正式/运动）
  - [x] 风格偏好参考（从衣橱偏好+形象分析结果获取）
  - [x] 搭配生成（从衣橱中选择可搭配的单品组合）
  - [x] 推荐结果展示（2-3 套方案，每套含搭配图示+说明）
- 主流程：
  1. 用户进入今日穿搭页 → 自动获取当前天气
  2. 用户选择场景（如未设置默认则用"通勤"）
  3. 系统结合天气+场景+风格偏好+衣橱数据 → 生成 2-3 套搭配
  4. 展示推荐结果 → 用户点击可查看搭配详情
  5. 用户选择一套出门或点击「换一批」
- 状态要求：
  - 默认态：显示当日推荐（含天气信息）
  - 空态：衣橱 < 5 件时 "衣橱不够丰富，请添加更多衣服以获得推荐"
  - 加载态："正在为你搭配..." + 加载动画
  - 错误态：天气获取失败时使用默认参数生成，标注"未获取到天气"
- 数据需求：天气数据（实时）+ 用户场景偏好 + 衣橱数据
- MVP 边界：天气 API + 场景选择 + 2-3 套推荐卡片
- 暂不实现：定位自动切换城市、历史推荐对比、搭配收藏夹、分享到社交媒体
- 验收标准：
  - [x] 进入「每日推荐」页面，页面正常渲染
  - [x] 展示至少 3 套推荐穿搭方案（卡片形式）
  - [x] 每套推荐包含搭配预览图、主要适用场景
  - [x] 推荐考虑天气因素（展示温度/天气状况）
  - [x] 推荐考虑用户风格偏好（与风格报告数据联动）
  - [x] 点击单套推荐可查看完整搭配详情
  - [x] 支持「换一批」重新生成推荐

## 3. 界面与交互规格

### 3.1 页面结构
- [x] 底部 Tab Bar：衣橱 / 今日穿搭 / 风格报告 / 形象分析 / 我的（5个Tab）
- [x] 首页-我的衣橱：默认入口，衣服卡片网格（2列）+ 右下角「+」按钮
- [x] 拍照录入：全屏相机取景框（或相册选择器）+ 识别结果确认页
- [x] 衣服详情/编辑：（简化版，暂未实现编辑功能）
- [x] 今日穿搭：顶部天气信息 + 场景选择器 + 推荐卡片列表
- [x] 风格报告：顶部标题 + 报告内容卡片 + 可视化图表
- [x] 形象分析：Tab 切换（肤色测试/身材分析）+ 测试问卷 + 结果展示
- [x] 我的/个人中心：用户头像 + 设置列表

### 3.2 核心组件
- [x] `ClothingCard`：图片 + 类别标签 + 颜色小圆点（衣橱列表项）
- [x] `AddButton`：右下角浮动「+」按钮，Primary #BE185D
- [x] `FilterBar`：分类筛选标签（全部/上衣/下装/鞋子/配饰）
- [x] `RecognitionResult`：识别结果卡片（类别/颜色/风格标签）
- [x] `OutfitCard`：穿搭方案卡片（多件搭配展示）
- [x] `ColorChart`：色系偏好饼图（Recharts）
- [x] `StyleBarChart`：风格占比柱状图
- [x] `AnalysisTabBar`：肤色/身材 Tab 切换
- [x] `EmptyState`：空态引导组件

### 3.3 状态设计
- [x] 默认态：正常展示内容
- [x] 空态：显示引导插图 + 可操作文案（如「还没有衣服，添加第一件吧」）
- [x] 加载态：骨架屏或 Loading 动画
- [x] 成功态：Toast 提示 + 状态更新
- [x] 错误态：友好错误提示 + 重试按钮

### 3.4 响应式要求
- [x] 移动端优先，最小宽度 320px（iPhone SE）
- [x] 375px（iPhone 14/15）默认 2 列网格
- [x] 触控热区 ≥ 44×44pt
- [x] Tab Bar 高度 56px + safe-area
- [x] 禁止横向滚动

## 4. 架构与模块边界

- [x] Next.js App Router + TypeScript，初始化脚手架
- [x] 移动端 Tab 导航布局（app/(tabs)/layout.tsx）
- [x] 路由结构：
  ```
  app/(tabs)/
    wardrobe/      # 衣橱 Tab
    daily-fit/     # 今日穿搭 Tab
    style-report/  # 风格报告 Tab
    profile/       # 形象分析 Tab
    my/            # 我的 Tab
  app/upload/      # 拍照录入（独立页面）
  app/layout.tsx   # 全局布局 + Tab Bar
  app/api/         # API Routes
  ```
- [x] 数据层：内存存储（当前 MVP 阶段），可升级为 IndexedDB
- [x] 状态管理：React useState + useEffect
- [x] API Route 代理所有外部 AI 服务（API Key 不暴露客户端）
- [x] 图片存储：Base64 内存存储（当前 MVP）

## 5. 设计系统

- [x] 读取 prd.md 与 ui-ux-pro-max，选择设计取向（温暖柔和 + Exaggerated Minimalism 风格）
- [x] 创建 design-system/穿搭密码/MASTER.md（颜色、字体、圆角、阴影、组件规范）
- [x] 将设计 token 落地到所选框架的样式系统（Next.js globals.css + Tailwind CSS）
- [x] 覆盖关键界面的默认态、空态、加载态、成功态和错误态

## 6. API 与集成

### 6.1 AI 图像识别（拍照录入）
- [x] API Route：POST /api/recognize（代理 AI 识别服务，当前为 mock）
- [ ] 确认 MiniMax M2 是否支持视觉输入（图片 Base64 传入）- 后续接入真实 AI

### 6.2 风格报告生成
- [x] API Route：POST /api/generate-style-report
- [x] 降级：报告生成失败返回静态兜底模板

### 6.3 每日穿搭推荐
- [x] API Route：POST /api/generate-outfit
- [x] 降级：推荐生成失败返回空列表 + 友好提示

### 6.4 天气 API
- [x] 使用 wttr.in（免费无需 Key）或降级默认天气
- [x] API Route：GET /api/weather?city=北京
- [x] 降级：请求失败默认"晴，20-25°C"

### 6.5 API Key 安全
- [x] 所有 API Key 存入 .env.local，不提交到 Git
- [x] 客户端通过 Next.js API Route 间接调用，不直接暴露 Key
- [x] 日志要求：记录入口、关键参数（脱敏）、成功/失败、耗时、错误原因

## 7. 平台能力与权限

- [x] 相机权限：调用 input[type=file] capture 属性
- [x] 相册权限：调用 input[type=file]
- [x] 权限拒绝：降级为手动选择
- [x] 隐私说明：形象分析页明确告知"照片仅用于肤色分析，不会上传至第三方"

## 8. 验证

- [x] 开发完成后运行 `npm run build`，TypeScript 编译通过
- [x] 验证页面在 375px 宽度下布局正常
- [x] 验证触控热区 ≥ 44×44pt
- [x] 验证空态、加载态、错误态和未配置态
- [x] 验证 Tab Bar 不被 Home Indicator 遮挡

## 9. 开发完成后更新 PRD

- [ ] 调用 update-prd Skill
- [ ] 基于实际代码创建或更新 prd/ 文档体系
- [ ] 对照原始 prd.md 总目标与 prd/ 已实现文档，把未完成差距写入本 TODO
- [ ] 将根目录 prd.md 归档为 prd.original.md
- [ ] 生成或更新 CLAUDE.md 和 AGENTS.md

## 10. 未完成目标 / 后续功能

- [ ] 衣物编辑页（修改已录入衣服）
- [ ] 删除衣服功能
- [ ] 批量导入/批量拍照
- [ ] 历史报告对比（与上周/上月对比）
- [ ] 报告分享功能
- [ ] 搭配收藏夹（outfit 集合）
- [ ] 分享到社交媒体
- [ ] 实时推送（每日固定时间推送）
- [ ] 搭配偏好学习（根据用户选择记录优化推荐）
- [ ] 品牌识别
- [ ] MiniMax AI 真实接入（当前为 mock 数据）
- [ ] IndexedDB 持久化存储（当前为内存存储）
- [ ] 真实图像识别（MiniMax Vision API）

## 开发进度

### 2026-05-30
- 本轮：完成首版 MVP 开发，包括衣橱管理、拍照录入、风格报告、形象分析、每日穿搭推荐
- 构建：npm run build 成功，TypeScript 编译通过
- 运行：npm run dev 启动成功，页面可访问 http://localhost:3000
- 状态：全量基础功能已实现，UI 组件完整

## TODO 卸货记录

> 每次将已完成块从 §2 归档到 `prd/` 时追加；规范见 `.claude/rules/todo-prd-archive.md`。

（暂无）